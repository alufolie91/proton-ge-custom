diff --git a/dlls/winewayland.drv/wayland_pointer.c b/dlls/winewayland.drv/wayland_pointer.c
index 27b05a1eb09..c8bae1b3402 100644
--- a/dlls/winewayland.drv/wayland_pointer.c
+++ b/dlls/winewayland.drv/wayland_pointer.c
@@ -929,6 +929,10 @@ static BOOL wayland_pointer_set_cursor_shape(HCURSOR hcursor)
         if (!pointer->wp_cursor_shape_device_v1) return FALSE;
     }
 
+    /* HACK: Some compositors do not support cursor shape v2, use v1 substitutes. */
+    if (shape == 35) shape = 13;
+    if (shape == 36) shape = 32;
+
     wp_cursor_shape_device_v1_set_shape(pointer->wp_cursor_shape_device_v1,
                                         pointer->enter_serial, shape);
 
