diff --git a/meson.build b/meson.build
index be1028f..7b6fbdf 100644
--- a/meson.build
+++ b/meson.build
@@ -114,6 +114,20 @@ add_project_arguments(vkd3d_compiler.get_supported_arguments([
     ]),
   language : 'c')
 
+add_project_arguments(vkd3d_compiler.get_supported_arguments([
+    '-Ofast',
+    '-flto=12',
+    '-march=native',
+    ]),
+  language : [ 'c', 'cpp' ])
+
+add_global_link_arguments(vkd3d_compiler.get_supported_link_arguments([
+    '-Ofast',
+    '-flto=12',
+    '-march=native',
+    ]),
+  language : [ 'c', 'cpp' ])
+
 if cpu_family == 'x86'
   add_global_link_arguments(vkd3d_compiler.get_supported_link_arguments([
       '-Wl,--kill-at',
